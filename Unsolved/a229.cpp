#include <iostream>
#include <vector>
#include <algorithm>

#define IO ios_base::sync_with_stdio(0) , cin.tie(0) , cout.tie(0);
#define pb push_back

using namespace std;

vector<int> v;

int main(){

    IO

    int n;
    cin >> n;

    string s = "";

    for(int i=0; i<n; i++) s += "(";
    for(int i=0; i<n; i++) s += ")";

    v.pb(n);

    while(v.size() != n){

        for(int i=0; i<v.size(); i++){

        }

    }

    return 0;

}